# Copia in .env.freellmapi e genera una chiave:
#   python -c "import secrets; print(secrets.token_hex(32))"
ENCRYPTION_KEY=replace-with-64-char-hex-key

PORT=3001

# Bind Docker publish (single-user: lascia 127.0.0.1)
HOST_BIND=127.0.0.1

# Opzionale: rate limit proxy
# PROXY_RATE_LIMIT_RPM=120
